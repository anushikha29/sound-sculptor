# from main import sp

# def fetch_user_data(user_id):
#     # Fetch user's top items, playlists, and saved albums
#     top_items = sp.current_user_top_tracks(limit=10)
#     playlists = sp.current_user_playlists(limit=50)
#     saved_albums = sp.current_user_saved_albums(limit=50)
    
#     # Process and return the data as needed
#     print(top_items, playlists, saved_albums)
    
# fetch_user_data()